export default function Loading() {
  return null
}
// This component is used to show a loading state while the directory data is being fetched.
// It returns null, meaning it does not render anything to the DOM.
// This is useful for keeping the UI responsive while data is being loaded in the background.